name: Build APK
on: [push, workflow_dispatch]

jobs:
  android-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build Process
        run: |
          npm init -y
          npm install @capacitor/cli@latest @capacitor/core@latest @capacitor/android@latest @capacitor/assets
          
          mkdir -p www
          [ -f "index.html" ] && mv index.html www/
          
          npx cap init "Momentum" "com.harshitnimje.momentum" --web-dir www
          npx cap add android
          
          # Force generate icons and overwrite existing ones
          if [ -f "icon.png" ]; then
            echo "✅ icon.png detected. Forcing icon replacement..."
            npx capacitor-assets generate --android --assetPath icon.png
          else
            echo "❌ ERROR: icon.png not found!"
            exit 1
          fi
          
          # Final sync to push icons into the Android project
          npx cap sync android
          
          # Clean and Build
          cd android && ./gradlew clean assembleDebug --no-daemon

      - name: Upload Result
        uses: actions/upload-artifact@v4
        with:
          name: Momentum-Build-v1
          path: android/app/build/outputs/apk/debug/app-debug.apk

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "latest-build"
          name: "Latest Momentum Build"
          body: "This is the latest automated build of your app."
          files: android/app/build/outputs/apk/debug/app-debug.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
